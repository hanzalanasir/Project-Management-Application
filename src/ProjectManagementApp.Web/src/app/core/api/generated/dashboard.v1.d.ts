/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/dashboard/summary": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Role-scoped summary tiles, including the caller's personal task slice
         * @description One round trip for the whole summary. Admin aggregates across all projects; a ProjectManager across projects they own; a TeamMember across projects they are a member of — with their **task and overdue tiles counting only tasks assigned to them** (personal view).
         *     **A caller with an empty visible scope receives 200 with every count zero** and every breakdown present-but-empty — never 403, never 404. Enum-keyed maps always contain **every** enum key, zeros included, so this is a stable typed contract rather than a variable dictionary.
         *     Values are computed **live per request** in v1; there is no staleness window. `overdueTaskCount` uses `due_date < today` evaluated in **UTC** and `status != Done` — both fixed, because 006 Reports must produce identical numbers for the same caller.
         */
        get: operations["getDashboardSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/dashboard/activity": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Paginated, role-scoped recent-activity feed
         * @description Turns 001's audit trail into a readable "what changed" feed, scoped to entities in the caller's visible-project set. Read **through `IActivityLogService`**, never by querying `activity_logs` directly — which inherits 001's guarantee that audit entries carry no secrets.
         *     Shows **all visible entries** (no mutation-only filter); 001–004 audit only meaningful writes, so there is no read-noise to exclude. Strictly read-only: there is no "mark as read" affordance, by design.
         *     Activity on a project the caller can no longer see (no longer owned, or deleted — audit rows survive) is **scoped out for non-Admins** and remains visible to Admin.
         */
        get: operations["getDashboardActivity"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @enum {string} */
        ProjectStatus: "Planning" | "Active" | "OnHold" | "Completed" | "Cancelled";
        /** @enum {string} */
        TaskStatus: "ToDo" | "InProgress" | "InReview" | "Done" | "Blocked";
        /** @description **Every key is required, zeros included.** A status with no rows appears as `0` rather than being omitted — this is what makes the payload a stable typed contract instead of a free-form dictionary, and it is why the frontend needs no null-guards. */
        ProjectStatusCounts: {
            Planning: number;
            Active: number;
            OnHold: number;
            Completed: number;
            Cancelled: number;
        };
        /** @description Every key required, zeros included — as above. */
        TaskStatusCounts: {
            ToDo: number;
            InProgress: number;
            InReview: number;
            Done: number;
            Blocked: number;
        };
        /** @description Tasks assigned to the caller, within their visible-project set. For a TeamMember this **is** the number shown in the summary's task tiles (personal view); for a manager it is typically empty. */
        PersonalTaskSummary: {
            assignedTotal: number;
            byStatus: components["schemas"]["TaskStatusCounts"];
            overdueCount: number;
        };
        DashboardSummary: {
            /**
             * Format: date-time
             * @description Computation time (UTC). Values are live per request.
             */
            generatedAt: string;
            /**
             * @description The role scope applied, for labelling the view. Not an authorization signal.
             * @enum {string}
             */
            scope: "Admin" | "ProjectManager" | "TeamMember";
            visibleProjectCount: number;
            projectsByStatus: components["schemas"]["ProjectStatusCounts"];
            tasksByStatus: components["schemas"]["TaskStatusCounts"];
            /** @description `due_date < today (UTC)` AND `status != Done`, within scope. **Must equal 006's overdue count for the same caller.** */
            overdueTaskCount: number;
            /**
             * Format: double
             * @description Done ÷ total tasks in scope. **`0` when there are no tasks** — never a divide-by-zero.
             */
            completionRate: number;
            /** @description Derived from `tasksByStatus.Blocked` — no additional query. */
            blockedTaskCount: number;
            /** @description Distinct headcount across visible projects — a user on several counts **once**. */
            visibleTeamMemberCount: number;
            personalTasks: components["schemas"]["PersonalTaskSummary"];
        };
        ActivityEntry: {
            /** Format: uuid */
            id: string;
            /** @description `null` for system/seed actions. */
            actorName?: string | null;
            /** @example TaskStatusChanged */
            action: string;
            /** @example Task */
            entityType: string;
            /** @example 9ac41f02-6d18-4c7b-9a2e-51f0c8e4b3e5 */
            entityId: string;
            /** Format: date-time */
            timestamp: string;
            /**
             * @description Never contains secrets — guaranteed by 001 at write time.
             * @example InProgress → InReview
             */
            changeSummary: string;
        };
        /** @description The shared `PagedResult<T>` envelope (shared-contracts §4). */
        PagedActivityEntry: {
            items: components["schemas"]["ActivityEntry"][];
            page: number;
            pageSize: number;
            /** @description **Scoped to the caller** — a TeamMember never learns the system-wide activity volume. */
            totalCount: number;
            totalPages: number;
        };
        /** @description RFC 7807 — the only error shape this API emits (ADR-0003, shared-contracts §1). */
        ProblemDetails: {
            /** Format: uri-reference */
            type?: string;
            title?: string;
            /** Format: int32 */
            status?: number;
            detail?: string;
            /** Format: uri-reference */
            instance?: string;
            traceId?: string;
        };
        ValidationProblemDetails: components["schemas"]["ProblemDetails"] & {
            errors?: {
                [key: string]: string[];
            };
        };
    };
    responses: {
        /** @description Authentication required, or the access token is missing/invalid/expired. */
        UnauthorizedProblem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["ProblemDetails"];
            };
        };
        /** @description Unexpected server error. Never contains a stack trace in production. */
        UnexpectedProblem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["ProblemDetails"];
            };
        };
    };
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getDashboardSummary: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's scoped summary. Same shape for an empty scope, with zeros. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DashboardSummary"];
                };
            };
            401: components["responses"]["UnauthorizedProblem"];
            500: components["responses"]["UnexpectedProblem"];
        };
    };
    getDashboardActivity: {
        parameters: {
            query?: {
                page?: number;
                /** @description Values above the maximum are **clamped**, not rejected (Constitution VI.4). */
                pageSize?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of activity within the caller's scope. Empty page when there is none. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedActivityEntry"];
                };
            };
            /** @description Non-numeric or negative paging values. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ValidationProblemDetails"];
                };
            };
            401: components["responses"]["UnauthorizedProblem"];
            500: components["responses"]["UnexpectedProblem"];
        };
    };
}
